[pytest]
testpaths = tests
markers =
    unit: fast isolated tests
    integration: multi-module tests
    slow: PyVRP LS, AILS, full rollout smoke
    gpu: requires CUDA
    cython: requires built cython_heuristics
    mtdvrp: multi-depot env only
addopts = -m "not slow and not gpu and not cython" -q
